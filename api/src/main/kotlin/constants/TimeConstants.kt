package com.constants

object TimeConstants {
    const val ONE_MINUTE: Long = 60 * 1000;
    const val FIVE_MINUTES: Long = 5 * ONE_MINUTE;
    const val ONE_DAY: Long = 24 * 60 * ONE_MINUTE;
    const val FIVE_DAYS: Long = 5 * ONE_DAY;
    const val ONE_HUNDRED_DAYS: Long = 100 * ONE_DAY;
}